@model IEnumerable<QuanLyHopDongVaKySo_API.Models.TemplateMinute>

@* <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" /> *@

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">

<div class="container-fluid">
    <div class=" block justify-between items-center p-4  mt-4 mb-6 bg-white rounded-2xl shadow-xl shadow-gray-200 lg:p-5 sm:flex">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <i class="ph-bold ph-house" style="font-size: 20px; margin: auto 5px;"></i>
                <li class="breadcrumb-item"> <a>Trang chủ</a></li>
                <li class="breadcrumb-item active" aria-current="page"> Danh sách biên bản mẫu</li>
            </ol>
        </nav>
        <h1 class="typeHead">TẤT CẢ DANH SÁCH</h1>
        <div class="row">
            <input type="search" class="src" placeholder="Tìm kiếm khách hàng" />
            <button class="add_Cus col-md-2 offset-md-7">
                <a asp-controller="BusinessStaff" asp-action="MinuteFormPage">
                   THÊM MẪU
                    <i class="ph-bold ph-plus"></i>
                </a>
            </button>
        </div>
    </div>
 
    <div class="listTable">
        <table class="table">
            <thead>
                <tr style="color: #8B909A;">

                    <th scope="col">ID</th>
                    <th scope="col">Ngày cập nhật</th>
                    <th scope="col">Tên biên bản</th>
                    <th scope="col" style="text-align:center;">Hành động</th>
                </tr>
            </thead>
            <tbody class="table-group-divider">


                @foreach (var item in Model)
                {
                    <tr>
                        <th scope="row">@item.TMinuteID </th>
                        <td>@item.DateAdded</td>
                        <td>@item.TMinuteName</td>
                        <td class="action">
                            <a class="button-design" asp-controller="BusinessStaff" asp-action="EditMinuteFormPage" asp-route-tMinuteId="@item.TMinuteID"><i class="ph-bold ph-pencil-simple-line"></i></a>
                            <a class="button-save" onclick="confirmDownload('@item.Base64File','@item.TMinuteName')"><i class="ph-bold ph-download-simple"></i></a>
                            <a class="button-delete" asp-controller="BusinessStaff" asp-action="DeleteTMinute" asp-route-tMinuteId="@item.TMinuteID" onclick="confirmDelete(event)"><i class="ph-bold ph-trash"></i></a>
                        </td>
                    </tr>
                }


            </tbody>
        </table>
    </div>
</div>
<script>

    function downloadPDF(base64String, fileName) {
        var byteCharacters = atob(base64String);
        var byteNumbers = new Array(byteCharacters.length);
        for (var i = 0; i < byteCharacters.length; i++) {
            byteNumbers[i] = byteCharacters.charCodeAt(i);
        }
        var byteArray = new Uint8Array(byteNumbers);
        var blob = new Blob([byteArray], { type: 'application/pdf' });

        var blobUrl = URL.createObjectURL(blob);

        var a = document.createElement('a');
        a.href = blobUrl;
        a.download = fileName;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
    }

     function confirmDownload(base64File, tMinuteName) {
        // Sử dụng SweetAlert để xác nhận tải xuống
        Swal.fire({
            title: 'Bạn có muốn tải xuống?',
            text: 'Hành động này sẽ tải xuống tệp PDF.',
            icon: 'question',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Tải xuống',
            cancelButtonText: 'Hủy'
        }).then((result) => {
            if (result.isConfirmed) {
                // Nếu người dùng xác nhận tải xuống, thực hiện hàm tải xuống
                downloadPDF(base64File, tMinuteName);
            }
        });
    }

    function confirmDelete(event) {
        event.preventDefault(); // Ngăn chặn chuyển hướng ngầm khi nhấp vào liên kết

        // Sử dụng SweetAlert để xác nhận xóa
        Swal.fire({
            title: 'Bạn chắc chắn muốn xóa?',
            text: 'Hành động này sẽ xóa vĩnh viễn.',
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#d33',
            cancelButtonColor: '#3085d6',
            confirmButtonText: 'Xóa',
            cancelButtonText: 'Hủy'
        }).then((result) => {
            if (result.isConfirmed) {

                $.ajax({
                    url: event.target.href, // Lấy đường dẫn từ thuộc tính href của liên kết
                    type: 'DELETE',
                    success: function (response) {
                        if (!response.success) {
                            // Nếu xóa thành công, hiển thị SweetAlert thông báo
                            showDeleteSuccessAlert();
                        } else {
                            // Nếu xóa không thành công, hiển thị SweetAlert thông báo lỗi
                            showDeleteErrorAlert();
                        }
                    },
                    error: function () {
                        // Nếu có lỗi trong quá trình xóa, hiển thị SweetAlert thông báo lỗi
                        showDeleteErrorAlert();
                    }
                });
            }
        });
    }

    function showDeleteSuccessAlert() {
        // Hiển thị SweetAlert với thông báo xóa thành công
        Swal.fire({
            icon: 'success',
            title: 'Thành công!',
            text: 'Dữ liệu đã được xóa thành công.',
            confirmButtonText: 'OK'
        }).then((result) => {
            // Chuyển hướng tới trang mong muốn sau khi người dùng nhấp OK
            var redirectToUrl = '@Url.Action("ListMinuteFormPage")';
            window.location.href = redirectToUrl;
        });
    }

    function showDeleteErrorAlert() {
        // Hiển thị SweetAlert với thông báo xóa không thành công
        Swal.fire({
            icon: 'error',
            title: 'Lỗi!',
            text: 'Đã xảy ra lỗi khi xóa dữ liệu.',
            confirmButtonText: 'OK'
        }).then((result) => {
            // Chuyển hướng tới trang mong muốn sau khi người dùng nhấp OK
            window.location.reload();
        });
    }
</script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>




